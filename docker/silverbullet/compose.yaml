services:
    silverbullet:
        image: ghcr.io/silverbulletmd/silverbullet
        restart: unless-stopped
        volumes:
            - ${DATA_PATH}:/space
        networks:
            - newt_network

networks:
    newt_network:
        external: true
